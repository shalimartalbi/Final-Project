This prototype simulates an integrated workspace that consolidates email and calendar workflows into a single interface. The application includes interactive features such as composing and sending emails, conversation threads, filtering (Unread, Important, Sent, Archive), meeting creation and cancellation, calendar event details, tool connection simulation, login/logout flows, and attachment downloads. All functionality is implemented using HTML, CSS, and JavaScript.# Final-Project
